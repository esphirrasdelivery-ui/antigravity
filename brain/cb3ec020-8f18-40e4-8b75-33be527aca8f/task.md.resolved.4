# Implementação do Ressuprimento por Nível (Par-Level)

- `[x]` 1. **Banco de Dados:** Criar coleção `chef_lojas_metas` no `db.js` e habilitar sincronização em tempo real.
- `[x]` 2. **Formulário Público (`pedido.html`):** Refatorar textos (ex: "Pedido" -> "Fechamento de Inventário", "Quantidades Necessárias" -> "Estoque Atual na Loja").
- `[x]` 3. **Painel de Configuração:** Criar Modal no `js/pedidos.js` para a Cozinha Central definir o Estoque Máximo por loja.
- `[x]` 4. **Motor de Cálculo (Romaneio):** Atualizar lógica de impressão para `(Estoque Máx - Estoque Atual)`. Itens <= 0 não devem ser impressos.
- `[x]` 5. **Módulo de Conferência (Scanner):** A meta de separação de cada item bipado agora é fruto desse mesmo cálculo.
- `[x]` 6. **Deploy Final:** Executar `build.ps1` e publicar nuvem via Netlify.
